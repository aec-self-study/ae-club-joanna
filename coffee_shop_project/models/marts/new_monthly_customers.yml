version: 2

models:
  - name: new_monthly_customers
    description: 'customers who ordered for the first time, aggregated per month'

    columns:
      - name: order_month
        tests:
          - unique
          - not_null

      - name: new_customers
        tests:
          - not_null